# 국내 상장 ETF 정보 수집 및 엑셀 출력 요구사항 및 설계 명세서

본 문서는 국내에 상장된 모든 ETF 정보를 수집하고 가치 있는 서식을 입혀 엑셀(`.xlsx`) 파일로 출력하는 파이썬 스크립트(`collect_etf_data.py`)를 개발하기 위한 상세 설계 문서입니다. 이 문서에 정리된 요구사항을 바탕으로 타 LLM 또는 개발자가 동일한 동작을 수행하는 파이썬 코드를 처음부터 완벽하게 재작성할 수 있습니다.

---

## 1. 개요 및 목적
국내 상장된 모든 ETF의 시세 정보, 기본 정보 및 자산운용사, 수수료, 분류 체계를 자동으로 수집합니다. 수집된 정보는 단일 워크시트(`Sheet1`)에 정렬하고, 사용자 가독성을 높일 수 있는 전문적인 엑셀 서식을 가공하여 파일로 내보냅니다.

- **데이터 소스**: 네이버 금융 (한국거래소의 보안 정책 우회 및 안정성 확보)
- **최종 출력 경로**: `./output/ETF_실행날짜(YYYYMMDD).xlsx` (예: `./output/ETF_20260605.xlsx`)
- **수집 아키텍처**: 전체 목록을 호출한 후 약 1,100여 개가 넘는 개별 종목 정보를 스레드 풀을 이용하여 병렬로 신속하게 수집합니다.

---

## 2. 세부 수집 프로세스 및 API 명세

### 2.1. 전체 ETF 기본 정보 수집 (API)
- **호출 URL**: `https://finance.naver.com/api/sise/etfItemList.nhn`
- **호출 방식**: HTTP GET
- **핵심 데이터 추출 필드**:
  - `itemcode` (종목코드, 예: `"069500"`)
  - `itemname` (종목명, 예: `"KODEX 200"`)
  - `etfTabCode` (네이버 금융 탭 분류 코드, 정수형 1~7)
  - `marketSum` (시가총액, 단위: 억원)
  - `quant` (거래량, 정수형)

### 2.2. 개별 ETF 상세 정보 수집 (크롤링)
각 ETF의 세부 항목(기초지수, 수수료, 상장일, 자산운용사)을 추출하기 위해 개별 페이지를 크롤링합니다.
- **상세 페이지 URL**: `https://finance.naver.com/item/main.naver?code={종목코드}`
- **호출 방식**: HTTP GET (헤더에 표준 브라우저 User-Agent 설정 필수)
- **대상 테이블 및 파싱 규칙**:
  1. **`summary="기초지수 정보"` 테이블**:
     - `<th>기초지수</th>` 행의 `<td>` 텍스트 추출 -> **기초지수명**
     - `<th>유형</th>` 행의 `<td>` 텍스트 추출 -> **유형 텍스트** (예: `"국내주식형, 대표지수"`)
     - `<th>상장일</th>` 행의 `<td>` 텍스트 추출 -> **상장일**
  2. **`summary="펀드보수 정보"` 테이블**:
     - `<th>펀드보수</th>`에 해당하는 `<td>` 텍스트 추출 -> **수수료** (예: `"연 0.15%"`)
     - `<th>자산운용사</th>`에 해당하는 `<td>` 텍스트 추출 -> **자산운용사** (예: `"삼성자산운용(주)"`)

### 2.3. 병렬 처리 규칙
- 다량의 HTTP 호출로 인한 속도 지연을 방지하기 위해 `concurrent.futures.ThreadPoolExecutor(max_workers=30)` 등의 라이브러리를 사용해 병렬로 상세 정보를 크롤링합니다.
- 전체 수집율과 진행률 표시를 위해 `tqdm` 라이브러리를 적용하여 콘솔에 진행 상태를 출력합니다.

---

## 3. 데이터 전처리 및 정제 규칙

### 3.1. 날짜 표준화
- 상세 페이지에서 추출한 상장일 텍스트(예: `"2002년 10월 14일"`)를 정규 표현식을 사용하여 **`YYYY-MM-DD`** 형식(예: `"2002-10-14"`)으로 정제합니다.

### 3.2. 수수료(보수) 표준화
- 보수 텍스트(예: `"연0.15%"` 또는 `"0.007%"`)에서 숫자값만 추출하여 **소수점 실수형(float)** 데이터로 변환합니다. (예: `0.15%` -> `0.0015`).
- 변환 실패 또는 데이터 누락 시 원본 텍스트를 유지하거나 `None` 처리합니다.

### 3.3. 분류 체계 매핑 규칙
수집된 정보를 조합하여 대분류, 중분류, 소분류 필드를 규칙적으로 매핑합니다.

1. **대분류유형**:
   - `etfTabCode`가 1, 2, 3인 경우: `"국내"`
   - `etfTabCode`가 4인 경우: `"해외"`
   - 그 외의 경우 상세 페이지의 `유형 텍스트`에 `"국내"`가 포함되면 `"국내"`, `"해외"`가 포함되면 `"해외"`로 지정하고, 판별이 안 될 경우 `"기타"`로 대입합니다.
2. **중분류유형**:
   - 네이버 탭 분류 규칙을 기반으로 하며 다음과 같이 정확히 6개 카테고리(`시장지수`, `업종/테마`, `파생`, `원자재`, `채권`, `기타`)로 통합 매핑합니다.
     - `etfTabCode`가 1인 경우 -> `"시장지수"`
     - `etfTabCode`가 2인 경우 -> `"업종/테마"`
     - `etfTabCode`가 3인 경우 -> `"파생"`
     - `etfTabCode`가 4(해외 주식)인 경우 -> 상세 `유형 텍스트`에 `"시장대표"`, `"대표지수"`, `"대표"` 키워드가 있으면 `"시장지수"`, 없으면 `"업종/테마"`로 지정
     - `etfTabCode`가 5인 경우 -> `"원자재"`
     - `etfTabCode`가 6인 경우 -> `"채권"`
     - `etfTabCode`가 7인 경우 -> `"기타"`
3. **소분류유형**:
   - ETF의 **종목명**에 특정 키워드가 들어있는지 스캔하고, 존재하는 키워드를 순서대로 조합(콤마 구분)하여 표시합니다. 검색할 키워드는 **`["TR", "액티브", "합성", "(H)", "인버스", "레버리지"]`**입니다.
   - 예: 종목명이 `"KODEX CD금리액티브(합성)"`이면 소분류유형은 `"액티브, 합성"`이 됩니다. 해당 키워드가 아예 없는 경우 빈 값(`""`)으로 비워둡니다.

---

## 4. 엑셀 쓰기 및 디자인 스타일링 규칙 (`openpyxl` 서식 설계)

### 4.1. 시트 구성 및 헤더 명세
- **시트 이름**: `"Sheet1"` (단일 시트로 구성)
- **컬럼 명세 및 정렬 방식**:

| 번호 | 컬럼명 | 내용 설명 | 데이터 정렬 | 서식 형식 (Number Format) |
| :--- | :--- | :--- | :--- | :--- |
| 1 | **종목코드** | ETF 티커 (6자리) | 가운데 정렬 | `@` (텍스트 강제) |
| 2 | **종목명** | ETF 명칭 | 왼쪽 정렬 | 일반 텍스트 |
| 3 | **자산운용사** | ETF 자산운용 회사명 | 왼쪽 정렬 | 일반 텍스트 |
| 4 | **대분류유형** | 국내/해외/기타 | 가운데 정렬 | 일반 텍스트 |
| 5 | **중분류유형** | 시장지수/업종테마/채권 등 | 가운데 정렬 | 일반 텍스트 |
| 6 | **소분류유형** | TR, 합성, 레버리지 등 키워드 | 가운데 정렬 | 일반 텍스트 |
| 7 | **기초지수명** | ETF가 추종하는 지수명 | 왼쪽 정렬 | 일반 텍스트 |
| 8 | **상장일** | YYYY-MM-DD 형식 날짜 | 가운데 정렬 | 일반 텍스트 |
| 9 | **시가총액(억원)** | 억원 단위 크기 숫자 | 오른쪽 정렬 | `#,##0` (천 단위 쉼표) |
| 10 | **거래량** | 거래량 정수 | 오른쪽 정렬 | `#,##0` (천 단위 쉼표) |
| 11 | **수수료** | 소수점 비율값 (Float) | 오른쪽 정렬 | `0.000%` (백분율 서식) |

### 4.2. 디자인 스타일 사양
1. **폰트**: 모든 셀의 폰트명을 `"Malgun Gothic"` (맑은 고딕)으로 설정하고 크기는 `10`으로 지정합니다.
2. **헤더 서식**:
   - 1행(헤더 행)의 높이를 `25`로 설정합니다.
   - 글자 색은 `"1E293B"`(어두운 감청색 계열), 굵게(bold) 효과를 줍니다.
   - 셀 배경 채우기 색(PatternFill)은 `"EDF2F7"`(옅은 그레이/블루 계열)로 지정합니다.
3. **데이터 행 서식**:
   - 데이터 행(2행부터 끝행까지)의 높이를 `20`으로 설정합니다.
4. **테두리 선(Borders)**:
   - 모든 데이터 셀 및 헤더 셀 사방에 `"CBD5E1"`(옅은 회색) 색상의 얇은(thin) 테두리를 적용합니다.
5. **엑셀 뷰 속성**:
   - 엑셀 시트에서 눈금선이 강제로 활성화되어 표시되도록 속성을 부여합니다.
     (`ws.views.sheetView[0].showGridLines = True`)
   - 사용자가 필터링하기 쉽도록 헤더 셀 전체에 오토필터(`AutoFilter`)를 적용합니다.
6. **열 너비 자동 맞춤**:
   - 각 열에 포함된 텍스트 중 가장 긴 길이에 맞게 열 너비(`column_dimensions.width`)를 자동 조정합니다.
   - **한글 자수 보정**: 영어/숫자는 1글자당 길이 1로 계산하고, 아스키코드 127을 초과하는 문자(한글)는 1글자당 길이 2로 보정하여 오차를 줄입니다.
   - 열 너비의 최솟값은 12로 제한하고, 가독성을 위해 계산된 최대 길이에 `+4` 크기의 여백을 추가 적용합니다.
